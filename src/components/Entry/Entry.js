import React from 'react';

import './Entry.css';

function Entry() {
  return (
    <div className="btn-container">
      <button>Sign up</button>
      <a href='#'>Login</a>
    </div>
  )
}

export default Entry;
